class itemTimeline {
  constructor(json, ano) {
    collectInfoFromJsonByYear(json, ano)


    this.logo_path = logo_path;
    this.descrição = descrição;
    this.titulo = titulo;
    this.estrela = estrela;
  }


  function(){
    for each json, if json.year == 2025
    
  }

  function getStar(){

    GET STARNETFLIX


  }

  //html
  <div class= "itemTimeline">
    <img src={this.logo_path}></img>
    <p>{{this.descricao}}</p>
    <p>{{this.titulo}}</p>
  </div>

}